<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
	</head>
	<body>
		<h1>Demande de
			{{ contact.name | capitalize }}
			{{ contact.lastname | upper }}</h1>

		<p>Email :
			{{contact.email }}</p>
		<p>Sujet :
			{{contact.subject}}</p>
		<div>
			<p>Message :</p>
			<p>{{contact.message}}</p>
		</div>
	</body>
</html>
